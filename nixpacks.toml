[phases.setup]
nixPkgs = [
    "ffmpeg",
    "python"
]

[phases.install]
commands = [
    "pip install -r requirements.txt"
]

[phases.build]
commands = [
    "mkdir -p downloads"  # Create download directory
]

[start]
cmd = "python app.py"
