providers = ["...", "python"]
[phases.setup]
nixPkgs = [
    "ffmpeg",
    "pip"
]

[phases.install]
commands = [
    "pip install -r requirements.txt"
]

[phases.build]
commands = [
    "mkdir -p downloads"  # Create download directory
]

[start]
cmd = "python app.py"
